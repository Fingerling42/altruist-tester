# Altruist Tester

Python-based post-assembly tester for Altruist devices.

## Development

```bash
uv sync
uv run altruist-tester --help
uv run python -m altruist_tester.cli --help
uv run pytest
```
